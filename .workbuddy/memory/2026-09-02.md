# 2026-09-02

## 个人网站（GitHub Pages / Jekyll）骨架搭建
- 在 `/Users/pamphile/WorkBuddy/个人网站/` 用 **Jekyll（主题无关、自定义布局）** 搭建个人项目展示站，已 `git init` 并首次提交。
- 关键文件：`_config.yml`（`baseurl`/`url` + projects 集合）、`_layouts/{default,project}.html`、`index.html`（自动汇总 `_projects/` 卡片）、`assets/css/style.css`、`Gemfile`（github-pages）、`README.md`（部署说明）。
- 已放两个项目页：`_projects/qusheng.md`（蛆生 v1.6）、`_projects/schedule.md`（单文件排程/笔记应用）。
- 决策：用户选 Jekyll 而非纯静态，看重"写 Markdown 自动生成列表"的省力。新增项目只需在 `_projects/` 丢一个 `.md`。
- 注意：本机 Ruby 2.6 过旧，无法本地 `bundle exec jekyll serve`；GitHub 服务端构建不受影响。上线需用户建仓库 + `git remote add` + `git push` + Settings→Pages 选 main 分支。
- 若仓库名非 `用户名.github.io`，需把 `_config.yml` 的 `baseurl` 改为 `/仓库名`。

## 部署上线（完成）
- GitHub 用户名 = **blackinkvc**（由 git 全局 user.name 推断并验证：`ssh -T` 返回 `Hi blackinkvc!`）。
- 仓库 `blackinkvc.github.io` 经 **GitHub API（PAT）** 创建；代码经 **SSH（ed25519 密钥，带口令）** 推送 `main` 分支成功。
- Pages 经 API 开启（source=main, /），状态 building，地址 https://blackinkvc.github.io/。
- 关键排错经验：
  - 系统 Ruby 2.6 太旧，本地 `jekyll serve` 跑不了；不影响 GitHub 服务端构建。
  - 沙箱非交互，SSH 密钥口令用 `SSH_ASKPASS` + `ssh-agent` 临时缓存解决（`/tmp/askpass.sh` 只 echo `$PASSPHRASE` 环境变量，口令不落盘）。
  - 建仓库走 GitHub REST API（需 PAT），SSH 密钥本身不能建仓库。
  - 创建仓库 POST 返回 201；POST /pages 返回 409 表示已启用（非错误）。
- 安全：PAT 用完应到 GitHub 撤销；SSH 密钥口令仍生效。
- 后续更新：在本目录改文件 → `git add/commit/push`（沙箱内每次 push 需上述口令缓存流程；用户本机 `ssh-add` 一次即可）。新增项目只需在 `_projects/` 加 `.md`。

## README 改为项目介绍（完成）
- 用户要求把仓库根 `README.md` 从「GitHub Pages 部署说明」改为「项目介绍」（蛆生 v1.6 + 单文件排程/笔记应用），已重写并推送（commit 9c90201）。
- 原部署说明内容移出 README；Jekyll 站点本身部署方式不变（GitHub Pages 自动构建）。

## 蛆生源文件查找（未果，需用户提供）
- 用户想把「蛆生」游戏本体直接部署到 GitHub Pages（替代失效的 workbuddy.link 预览）。经查：本地 `/Users/pamphile/WorkBuddy`（仅个人网站）、Gitee `space-migration-manual`（空仓库）、资料库「我的文档」（仅示例 doc）、失效链接（404「链接已失效」）四处均**未找到源文件**。
- 资料库（云空间）全库搜索「蛆生/qusheng/v1.6/文字冒险/游戏/饱食度/转世」均无命中；personal 空间根目录仅 1 个示例 doc。
- 结论：源文件不在自动可访问位置，需用户从 WorkBuddy 客户端导出/提供，并明确在云空间的具体位置（空间名/文件夹/文件名），或把文件放到本地站点目录。
- 待办：`_projects/qusheng.md` 的 `link` 仍指向已失效的 workbuddy.link，源文件部署后应改为站内路径（如 `/qusheng/index.html` 之类，取决于部署方式）。

## 远程地址改为 HTTPS（完成）
- 用户决定改用 GitHub Desktop 管理仓库，已将 remote 从 SSH（`git@github.com:...`）改为 HTTPS（`https://github.com/blackinkvc/blackinkvc.github.io.git`）。
- 目的：GitHub Desktop 用登录账号 OAuth 认证，免 SSH 口令。`git ls-remote` 验证可达（返回 main）。
- 影响：之后以 GitHub Desktop 为主；沙箱命令行若再 push 需 HTTPS 凭证（GitHub 账号/PAT），不再走 SSH 口令缓存流程。用户在 GitHub Desktop 登录态会把凭证存入 macOS 钥匙串，Push 免交互。

## 添加截图中的 8 个新项目占位页（本地 commit，待 push）
- 用户发来 WorkBuddy 项目会话列表截图，要求"把这些项目都放上去"。本地/Gitee/资料库均找不到这些项目的源文件，先按截图标题在 `_projects/` 创建占位展示页。
- 新增 8 个：`agent-status`（Agent 状态显示小组件）、`diary`（日记）、`qusheng-multiplayer`（蛆模拟器联网网页版）、`beichen-rogue`（北辰剑宗肉鸽选择）、`version-selector`（网页底部版本选择器）、`fantasy-world`（基于模板制作奇幻世界）、`human-skills`（人类基础技能包）、`cyberpunk-web` / `cyberpunk-wiki`（赛博工程补完计划 · 网页/维基）。
- 加上已有的 `qusheng`（蛆生 v1.6）、`schedule`（单文件排程/笔记应用），`_projects/` 下共 10 个项目卡片，首页会自动列出。
- 当前 commit `4283605` 在本地 main 分支，需用户用 GitHub Desktop push 到 GitHub 后才会上线。
- 占位页正文均为"待补充"，后续可由用户逐步填入内容或导出源文件部署。
